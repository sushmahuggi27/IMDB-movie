{{ include('include/headers.html.twig')}}
 <link href="{{ asset('') }}" rel="stylesheet" />
    <div class ="container">
    <h3>Actors List</h3>
    <a href="{{absolute_url(asset(''))}}addactors" class="btn btn-primary"> Add Actors</a>
    <a class="btn btn-secondary" href="{{absolute_url(asset(''))}}editmovies">GO BACK </a>
        <table class="table table-striped table-hover ">
          <link href="{{ asset('') }}" rel="stylesheet" />
      <head>
        <tr>
          <th>ACTOR ID</th>
          <th>NAME</th>
          <th>GENDER</th>
          <th>EMAIL</th>
          <th>PROFILE</th>
          <th>UPDATE</th>
          <th>DELETE</th>
        </tr>
      </thead>
      <tbody>
      {%for actors in actors%}
        <tr>
          <td>{{actors.id}}</td>
          <td>{{actors.name}}</td>
          <td>{{actors.gender}}</td>
          <td>{{actors.email}}</td>
          <td><img  src="{{absolute_url(asset(''))}}ActorsImage/{{actors.profile}}" width="30%" height="30%"></td>  
          <td><a href="{{absolute_url(asset(''))}}updateactors/{{actors.id}}" class="btn btn-primary">UPDATE</a></td>
          <td><a href="{{absolute_url(asset(''))}}deleteactors/{{actors.id}}" class="btn btn-primary">DELETE</a><td>
              </tr>
        {%else%}
            <tr>
                <td>NO RECORDS FOUND</td>
            <tr>
       {%endfor%}
       
      </tbody>
    </table>
    </div>