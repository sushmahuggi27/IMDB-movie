{{ include('include/headers.html.twig')}}
<body>
<div class ="container">
<div class="card mb-3">
 <div class="card-link">
  
    <a href="{{absolute_url(asset(''))}}addexisting" class="btn btn-warning"> Add Movies</a>
   
    <a href="{{absolute_url(asset(''))}}addactors" class="btn btn-primary"> Add Actors</a>
    <a href="{{absolute_url(asset(''))}}editactors" class="btn btn-warning"> View Actors</a>
      
    <a href="{{absolute_url(asset(''))}}adddirector" class="btn btn-primary"> Add Director</a>
    <a href="{{absolute_url(asset(''))}}editdirector" class="btn btn-warning"> View Director</a>

    <a href="{{absolute_url(asset(''))}}addproduction" class="btn btn-primary"> Add Production</a>
    <a href="{{absolute_url(asset(''))}}editproduction" class="btn btn-warning"> View Production </a>

    <a href="{{absolute_url(asset(''))}}addmedia" class="btn btn-primary"> Add Media</a>
    <a href="{{absolute_url(asset(''))}}editmedia" class="btn btn-warning"> View Media</a>
</div>

<h2 class="card-header"style ="text-align:center;"> All Movies Details </h2>
{%for movies in movies%}
<div class="card-body">
</div>
<!--<h4 class="card-subtitle mb-2 text-muted" 
  style ="font-size:30px;text-align:center; background-color: aliceblue;font-family: Arial, Helvetica, sans-serif;">
  Movie Title : {{movies.title}}
</h4> 
  <div class="card-body">
    <p class="card-text" style ="font-size:25px;text-align:center;" >About:<br></p>
  </div>
  <ul class="list-group list-group-flush" style ="font-size:20px;text-align:center;">
    <li class="list-group-item">Movie Id : {{movies.id}}</li>
    <li class="list-group-item">Release year : {{movies.relDate}}</li>
    <li class="list-group-item">Language : {{movies.language}}</li>
    <li class="list-group-item">Type : {{movies.type}}</li>
    <li class="list-group-item">Description : {{movies.description}}</li>
    <li><img  src="{{absolute_url(asset(''))}}MoviesImage/{{movies.poster}}" width="30%" height="30%"></li>
    <li class="list-group-item"><a href="{{absolute_url(asset(''))}}viewmore/{{movies.id}}" class="btn btn-primary">View More </a></li>
    <li class="list-group-item"><a href="{{absolute_url(asset(''))}}updatemovies/{{movies.id}}" class="btn btn-primary">UPDATE </a></li>
    <li class="list-group-item"><a href="{{absolute_url(asset(''))}}deletemovies/{{movies.id}}" class="btn btn-primary">DELETE</a></li>
    
  </ul>
  {%else%}
       <ul class="list-group list-group-flush">
       <li class="list-group-item">NO RECORDS FOUND</li>
  </ul>
   {%endfor%} -->

   <table class="table table-striped table-hover ">
        <link href="{{ asset('') }}" rel="stylesheet" />
      <head>
        <tr>
        <th>Movie ID</th>          
          <th>Movie Title</th>
          <th>Release year</th>
          <th>Language</th>
          <th>Type </th>
          <th>Description</th>
          <th>Poster</th>
          <th>View More</th>
          <th> Update</th>
          <th> Delete</th>
        </tr>
      </thead>
      <tbody>
      {%for movies in movies%}
        <tr>
        <td>{{movies.id}}</td>
          <td>{{movies.title}}</td>
          <td>{{movies.relDate}}</td>
          <td>{{movies.language}}</td>
          <td>{{movies.type}}</td>
          <td>{{movies.description}}</td>
          <td><img  src="{{absolute_url(asset(''))}}MoviesImage/{{movies.poster}}" width="35%" height="35%"></td> 
          <td><a href="{{absolute_url(asset(''))}}viewmoreadmin/{{movies.id}}" class="btn btn-primary">View More</a></td> 
          <td><li class="list-group-item"><a href="{{absolute_url(asset(''))}}updatemovies/{{movies.id}}" class="btn btn-primary"> UPDATE </a></li></td>
          <td><li class="list-group-item"><a href="{{absolute_url(asset(''))}}deletemovies/{{movies.id}}" class="btn btn-warning"> DELETE</a></li></td>
        </tr>
      {%else%}
        <tr>
          <td>NO RECORDS FOUND</td>
        <tr>
      {%endfor%}

      <ul class="pagination pagination-lg">
    {% if maxPages > 1 %}
    <ul class="pagination pagination-lg">
      {% if thisPage > 1 %}
    <li class="page-item">
      <a class="page-link" href="{{ path('show_movie_route', {currentPage: thisPage-1 < 1 ? 1 : thisPage-1}) }}">&laquo;</a>
     
    </li>
     
     {% endif %}
     {% for i in 1..maxPages %}
    <li class=" page-item active">
      <a class="page-link  " href="{{ path('show_movie_route', {currentPage:i}) }}">{{i}}</a>
      </li>
      {% endfor %}
      {%if thisPage < maxPages %}
    <li class="page-item">
      <a class="page-link" href="{{ path('show_movie_route',{currentPage: thisPage+1 <= maxPages ? thisPage+1 : thisPage})}}">&raquo;</a>
    </li>
    {% endif %} 
  </ul>
  {% endif %}
       
    </tbody> 
</div>
</div>
</body>