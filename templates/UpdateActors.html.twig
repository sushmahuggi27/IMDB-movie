{{ include('include/headers.html.twig')}}
{% form_theme form 'bootstrap_4_layout.html.twig'%}
<link href="{{ asset('') }}" rel="stylesheet" />
<div class ="container">
    <h3>Update Actors Details</h3>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
        <img  src="{{absolute_url(asset(''))}}ActorsImage/{{actors}}" width="5%" height="5%">
        <a class="btn btn-secondary" href="{{absolute_url(asset(''))}}editactors">GO BACK </a>
</div>