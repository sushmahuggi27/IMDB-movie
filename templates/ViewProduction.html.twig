{{ include('include/headers.html.twig')}}
 <link href="{{ asset('') }}" rel="stylesheet" />
    <div class ="container">
      <h3>Production Houses List</h3>
      <a href="{{absolute_url(asset(''))}}addproduction" class="btn btn-primary"> Add Production</a>
      <a class="btn btn-secondary" href="{{absolute_url(asset(''))}}editmovies">GO BACK </a>
        <table class="table table-striped table-hover ">
          <link href="{{ asset('') }}" rel="stylesheet" />
          <head>
            <tr>
              <th>PRODUCTION ID</th>
              <th>NAME</th>
              <th>COUNTRY</th>
              <th>DESCRIPTION</th>
              <th>UPDATE</th>
              <th>DELETE</th>
            </tr>
          </thead>
         <tbody>
          {%for production in production%}
            <tr>
              <td>{{production.id}}</td>
              <td>{{production.name}}</td>
              <td>{{production.country}}</td>
              <td>{{production.description}}</td>
              <td><a href="{{absolute_url(asset(''))}}updateproduction/{{production.id}}" class="btn btn-primary">UPDATE</a></td>
              <td><a href="{{absolute_url(asset(''))}}deleteproduction/{{production.id}}" class="btn btn-primary">DELETE</a><td>
            </tr>
          {%else%}
            <tr>
              <td>NO RECORDS FOUND</td>
            <tr>
          {%endfor%}
         </tbody>
       </table>
    </div>